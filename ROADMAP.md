# תוכנית המשך — QR Studio

המצב היום: מחולל קודי QR **סטטי** — המשתמש נותן קישור, מקבל תמונה. כל מה שקורה
קורה בדפדפן, ואין שרת, אין חשבון ואין מצב שנשמר מעבר ל-localStorage.

זו נקודת פתיחה טובה, אבל היא גם התקרה: ברגע שהקוד הודפס על שלט או על תפריט,
הוא נעול לנצח, ואי אפשר לדעת אם מישהו בכלל סרק אותו. שתי המגבלות האלה הן
הבסיס לכל מה שלמטה.

הסעיפים מסודרים לפי **ערך חלקי מאמץ**, לא לפי סדר כרונולוגי.

---

## שלב 1 — הרחבות שלא דורשות שרת

הכי זול לבנות, ומכפיל את מספר המקרים שהכלי מכסה. הכול נשאר צד-לקוח.

### 1.1 סוגי תוכן מעבר לקישור — ✅ בוצע

מומש: 10 סוגים (קישור, טקסט, WiFi, וואטסאפ, כרטיס ביקור, טלפון, אימייל, SMS,
מיקום, אירוע), מתוארים כדאטה ב-`lib/qr/content.ts` עם טופס גנרי אחד. כולם
מכוסים ב-`npm run test:content` — קידוד לפי התקן ופענוח חוזר.

התיאור המקורי:

| סוג | מה זה נותן | פורמט |
|-----|------------|--------|
| **WiFi** | אורח סורק ומתחבר לרשת בלי להקליד סיסמה | `WIFI:T:WPA;S:<ssid>;P:<pass>;;` |
| **כרטיס ביקור** | שמירת איש קשר בנגיעה אחת | vCard 3.0 |
| **וואטסאפ** | פתיחת צ׳אט עם הודעה מוכנה מראש | `wa.me/972...?text=` |
| **טלפון / SMS / מייל** | חיוג או הודעה מוכנה | `tel:` `sms:` `mailto:` |
| **אירוע ביומן** | הוספת אירוע ליומן | iCal `VEVENT` |
| **מיקום** | פתיחת ניווט לנקודה | `geo:lat,lon` |

המימוש: טאב סוגים מעל שדה הקלט, וטופס ייעודי לכל סוג שמייצר את המחרוזת.
המנוע הגאומטרי לא משתנה בכלל — הוא כבר מקבל מחרוזת שרירותית.

**מאמץ:** בינוני. **ערך:** גבוה מאוד.

### 1.1b הסרת רקע מלוגו — ✅ בוצע

מילוי משטח מהשוליים עם קצה רך וביטול שזירה, בשלוש עוצמות. רץ בדפדפן, ללא שרת
וללא מודל. מכוסה ב-`npm run test:logo`.

### 1.2 ייצור באצווה מקובץ — ✅ בוצע

מומש: הדבקת רשימה או העלאת CSV/TSV, ייצור כל הקודים באותו עיצוב, ושתי תוצאות
אפשריות — **גיליון A4** עם רשת קודים וקווי חיתוך, או **ארכיון ZIP** של קובצי
PNG נפרדים.

מה שנבנה בדרך:

- `lib/batch/csv.ts` — זיהוי מפריד אוטומטי (פסיק / פסיק-נקודה / טאב), פירוק עם
  מרכאות, וזיהוי כותרת לפי מילים מוכרות בעברית ובאנגלית בלבד. ניחוש "נראה כמו
  כתובת" נדחה בכוונה: הוא היה מוחק את הפריט הראשון של כל מי שהדביק רשימת שמות.
- `lib/zip.ts` — כותב ZIP ידני (store, בלי דחיסה — PNG כבר דחוס). הוספת ספרייה
  הייתה מוסיפה עשרות KB לטובת מבנה של כמה עשרות שורות.
- `lib/pdf.ts` — הורחב מעמוד-ותמונה-אחת למסמך מרובה עמודים ותמונות, עם קווי
  חיתוך מקווקווים.
- `lib/batch/sheet.ts` — פריסת A4 בארבע צפיפויות, שומרת על יחס הגובה של הקוד
  (כיתוב מוסיף גובה) וממרכזת את הרשת.

הכיתוב נצרב לתוך הרסטר של הקוד ולא נכתב כטקסט ב-PDF — כך אין צורך בהטמעת גופן
עברי, והכיתוב נראה זהה לתצוגה המקדימה.

**האזהרה שמוצגת** נמדדת על גודל המודול הבודד במ״מ (`cellWidth / boardSize`) ולא
על גודל המדבקה: מדבקה של 27 מ״מ עם כתובת קצרה נסרקת בקלות, ואותם 27 מ״מ עם
כתובת ארוכה כפול מודולים — לא. `npm run test:batch-scan` מפענח כל צפיפות
ברזולוציה שמצלמה קולטת בגודל הזה, ונכשל אם קוד לא נקרא **בלי** שהאזהרה הופיעה.

### 1.3 PDF וקטורי אמיתי

היום ה-PDF מטמיע תמונה. לדפוס מקצועי רוצים וקטור — נתיבים אמיתיים שאפשר
להגדיל בלי הגבלה ולהפריד לצבעים. הגאומטריה כבר קיימת כנתיבי SVG, וההמרה
לאופרטורים של PDF (`m`, `l`, `c`, `re`, `f`) היא תרגום ישיר.

בונוס: תמיכה ב-CMYK ובקווי חיתוך (bleed) לבתי דפוס.

**מאמץ:** בינוני. **ערך:** בינוני — קריטי למשתמשים מסחריים.

### 1.4 שיתוף עיצוב בקישור

קידוד כל מצב העיצוב לפרמטר בכתובת. מאפשר "תשלח לי את העיצוב שלך", שמירת
מועדפים בסימניות, ותבניות מוכנות לארגון.

**מאמץ:** נמוך. **ערך:** בינוני.

### 1.5 שיפורי עיצוב

- ~~**חילוץ פלטה מהלוגו**~~ — ✅ בוצע (`lib/logo/palette.ts`).
- **צבע נפרד לכל אחת משלוש העיניים** — הבסיס כבר קיים בטיפוסים.
- **רקעים מעוצבים** — דפוס עדין או גרדיאנט מאחורי הקוד, במקום צבע אחיד.
- **מסגרת סביב הקוד** ולא רק כיתוב מתחתיו.
- ~~**הגרלת עיצוב**~~ — ✅ בוצע (`lib/qr/shuffle.ts`), עם סף הניגודיות כשומר.
- **ספריית אייקונים מובנית** ללוגו, למי שאין קובץ.

**מאמץ:** נמוך-בינוני לכל אחד. **ערך:** בינוני.

### 1.6 סורק מובנה

מצלמה בתוך האפליקציה לבדיקת הקוד בשטח, אחרי הדפסה. `BarcodeDetector`
כבר בשימוש ב-`verify.ts`, אז רוב התשתית קיימת.

**מאמץ:** נמוך. **ערך:** בינוני — סוגר את הלולאה בין "יצרתי" ל"עובד".

---

## שלב 2 — קודים דינמיים ⭐⭐ המהלך המשמעותי ביותר

**הבעיה שזה פותר:** קוד QR מודפס הוא נצחי. אם הקישור השתנה, אם הדף נמחק, אם
הקמפיין הסתיים — הקוד מת, וכל מה שהודפס הולך לפח.

**הפתרון:** הקוד מצביע על `qr-generator-92987.web.app/r/<id>` במקום על היעד
עצמו. השרת מפנה ליעד הנוכחי, שאפשר לשנות מתי שרוצים.

מה זה פותח:
- **החלפת יעד אחרי הדפסה** — בלי להדפיס מחדש.
- **סטטיסטיקות סריקה** — כמה, מתי, מאיפה, איזה מכשיר.
- **תפוגה ותזמון** — קוד שפעיל רק בטווח תאריכים.
- **הפניה לפי מכשיר** — אייפון ל-App Store, אנדרואיד ל-Play.
- **בדיקת A/B** — שני יעדים, חלוקה אקראית.

### ארכיטקטורה

```
Firestore
  links/{id}     → { target, ownerId, createdAt, active, expiresAt, rules }
  scans/{id}     → { linkId, ts, country, deviceType, referrer }

Cloud Function (או Hosting rewrite)
  GET /r/:id     → קורא את היעד, כותב אירוע סריקה, מחזיר 302
```

### מה צריך ממך

הרחבת פרויקט ה-Firebase הקיים:

1. **הפעלת Firestore** בקונסולה (מצב production).
2. **הפעלת Cloud Functions** — דורש שדרוג לתוכנית **Blaze** (תשלום לפי שימוש).
   בפועל, בנפחים קטנים זה נשאר בשכבה החינמית ועולה אפס.
   אם עדיף להימנע מ-Blaze, אפשר לממש את ההפניה גם דרך Hosting rewrite לפונקציה
   ב-Cloud Run, או דרך ספק חיצוני — אבל Functions היא הדרך הפשוטה.
3. **כללי אבטחה ל-Firestore** — אכתוב אותם, צריך רק לאשר פריסה.

### שיקולי פרטיות

היום האפליקציה מבטיחה במפורש שהקישור לא נשלח לשום שרת. קוד דינמי **שובר**
את ההבטחה הזאת מעצם טבעו. לכן:

- שני מצבים מפורשים בממשק: **קוד סטטי** (כמו היום, פרטי לגמרי) מול
  **קוד דינמי** (נשמר בשרת, ניתן לעריכה ולמדידה).
- הסטטי נשאר ברירת המחדל.
- מדידת סריקות בלי לשמור IP — רק מדינה, סוג מכשיר וחותמת זמן.

**מאמץ:** גבוה. **ערך:** גבוה מאוד — זה מה שהופך כלי חד-פעמי למוצר עם שימוש חוזר.

---

## שלב 3 — חשבון וספרייה

נגזר משלב 2, ולמעשה תנאי לו: אם יש קודים דינמיים, צריך למי הם שייכים.

- **Firebase Auth** — התחברות עם Google, ובנוסף התחברות אנונימית כדי שלא
  לחסום את הכלי מאחורי הרשמה.
- **ספריית קודים בענן** — במקום ההיסטוריה ב-localStorage. סנכרון בין מכשירים.
- **תיקיות ותגיות**, חיפוש, שכפול קוד קיים.
- **תבניות צוות** — עיצוב מותג שמור, שכל מי שבארגון מתחיל ממנו.

**מאמץ:** בינוני-גבוה. **ערך:** גבוה, אבל רק אחרי שלב 2.

---

## שלב 4 — איכות ותשתית

לא נראה למשתמש, אבל קובע כמה מהר אפשר להמשיך לפתח.

### בדיקות
- ~~**בדיקות יחידה** (Vitest)~~ — ✅ בוצע: 98 בדיקות על המודולים הטהורים.
- ~~**`scan-test` ב-CI**~~ — ✅ בוצע: `.github/workflows/ci.yml` מריץ טיפוסים,
  יחידה, בנייה, ואז את כל בדיקות הדפדפן.
- **בדיקות ויזואליות** — השוואת צילומי מסך של הגלריה, כדי שעיצוב לא "יזוז" בשקט.
- **בדיקת נגישות אוטומטית** (axe) בצינור ה-CI.

### ביצועים
- **Web Worker** לחישוב הגאומטריה — היום קוד ארוך במיוחד יכול להקפיא את הממשק
  לרגע בזמן הקלדה.
- **OffscreenCanvas** לייצוא בגדלים גדולים (4096px, A4), שלא יחסום את ה-UI.

### בינלאומיות
- **תשתית i18n** — היום המחרוזות מוטמעות בקוד. חילוץ לקובץ אחד יאפשר אנגלית
  וערבית כמעט בחינם. ה-RTL כבר בנוי נכון, אז ערבית תעבוד מיד.

**מאמץ:** בינוני. **ערך:** גבוה לטווח ארוך.

---

## הצעה לסדר עבודה

| # | מה | למה קודם |
|---|-----|-----------|
| 1 | סוגי תוכן (1.1) | הכי הרבה ערך ליחידת מאמץ, ואפס סיכון ארכיטקטוני |
| 2 | ~~בדיקות יחידה + CI (4)~~ ✅ | לפני שהקוד גדל, בזמן שהוא עוד קטן |
| 3 | ~~חילוץ פלטה + הגרלה (1.5)~~ ✅ | "רגעי וואו" זולים שמייחדים את הכלי |
| 4 | **ייצור באצווה + גיליון מדבקות (1.2)** ← הבא | פותח שימוש עסקי אמיתי |
| 5 | קודים דינמיים (2) | המהלך הגדול — אחרי שהבסיס יציב ובדוק |
| 6 | חשבון וספרייה (3) | הגיוני רק אחרי 5 |

---

## שאלות פתוחות שכדאי להחליט עליהן

1. **מי המשתמש?** אדם פרטי שצריך קוד לפוסט, או עסק שמדפיס תפריטים? התשובה
   משנה סדרי עדיפויות — אצווה ודפוס מול עיצוב ורשתות.
2. **מודל עסקי?** אם התשובה היא "בחינם תמיד", שלב 2 עולה כסף בלי החזר, וכדאי
   להגביל את מספר הקודים הדינמיים למשתמש.
3. **דומיין משלך?** לקודים דינמיים אורך הכתובת משפיע ישירות על צפיפות הקוד:
   `qr-generator-92987.web.app/r/abc123` מייצר קוד צפוף בהרבה מ-`qr.co.il/abc123`.
   דומיין קצר הוא שיפור טכני אמיתי, לא רק מיתוג.
