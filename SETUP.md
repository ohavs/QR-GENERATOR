# הפעלת הקודים הדינמיים

הקוד כבר כתוב ונפרס. מה שנשאר הוא **ארבע פעולות בקונסולה** שרק אתה יכול לעשות,
כי הן דורשות בעלות על הפרויקט.

הפרויקט: `qr-generator-92987`

---

## שלב 1 — שדרוג לתוכנית Blaze

**חובה.** פונקציות ענן לא זמינות בתוכנית החינמית.

🔗 https://console.firebase.google.com/project/qr-generator-92987/usage/details

לחץ **Modify plan** ← **Blaze** ← חיבור חשבון חיוב.

**כמה זה יעלה?** בנפח קטן — אפס. התוכנית כוללת מכסה חינמית נדיבה שנשארת בתוקף:

| שירות | חינם עד | מה זה אומר |
|-------|---------|-------------|
| פונקציות | 2 מיליון קריאות בחודש | 2 מיליון סריקות |
| מסד נתונים — קריאות | 50,000 ליום | כל סריקה = קריאה אחת |
| מסד נתונים — כתיבות | 20,000 ליום | כל סריקה = 2 כתיבות |

בפועל תגיע לתקרה סביב **10,000 סריקות ביום**. מתחת לזה החשבון הוא אפס.

**מומלץ בחום:** אחרי השדרוג, הגדר התראת תקציב כדי שלא תהיה הפתעה:

🔗 https://console.cloud.google.com/billing/budgets?project=qr-generator-92987

**Create budget** ← סכום למשל 20 ש״ח ← התראה ב‑50% וב‑100%.

---

## שלב 2 — הפעלת מסד הנתונים

🔗 https://console.firebase.google.com/project/qr-generator-92987/firestore

לחץ **Create database**:

1. **Location** — בחר `eur3 (europe-west)` או `europe-west1`. לא ניתן לשנות אחר כך.
2. **Mode** — בחר **Production mode**. כללי האבטחה כבר כתובים בקוד ויידרסו את ברירת המחדל בפריסה.

---

## שלב 3 — הפעלת ההתחברות

🔗 https://console.firebase.google.com/project/qr-generator-92987/authentication/providers

לחץ **Get started**, ואז הפעל **שתי** שיטות:

- **Google** — הפעל, בחר אימייל תמיכה, שמור.
- **Anonymous** — הפעל, שמור. זה מה שמאפשר לשמור קודים בלי לכפות הרשמה.

אחר כך ודא שהדומיין מורשה:

🔗 https://console.firebase.google.com/project/qr-generator-92987/authentication/settings

תחת **Authorized domains** צריך להופיע `qr-generator-92987.web.app`. אם לא — הוסף.

---

## שלב 4 — פריסה

מהתיקייה של הפרויקט:

```bash
npm install
cd functions && npm install && cd ..

firebase deploy --only firestore:rules,firestore:indexes,functions,hosting
```

הפריסה הראשונה של הפונקציות לוקחת 2–3 דקות ועשויה לבקש הרשאות נוספות — אשר אותן.

---

## בדיקה שהכול עובד

1. פתח את האתר ולחץ על **קוד דינמי** ← התחבר ← צור קוד עם יעד כלשהו.
2. פתח את הכתובת הקצרה שקיבלת (`.../r/xxxxx`) — היא אמורה להפנות ליעד.
3. חזור לאפליקציה — מונה הסריקות אמור לעלות.
4. שנה את היעד, ופתח שוב את אותה כתובת — היא אמורה להוביל למקום החדש.

אם משהו נכשל:

```bash
firebase functions:log --only redirect
```

---

## מה נפרס בפועל

| רכיב | קובץ | תפקיד |
|------|------|-------|
| פונקציית ההפניה | `functions/index.js` | מקבלת `/r/<מזהה>`, מפנה ליעד, רושמת סריקה |
| כללי אבטחה | `firestore.rules` | כל אחד רואה ועורך רק את הקודים שלו |
| אינדקס | `firestore.indexes.json` | שליפת הקודים של משתמש לפי תאריך |
| ניתוב | `firebase.json` | `/r/**` מגיע לפונקציה, כל השאר לאפליקציה |

---

## החלטות שכדאי להכיר

**מהירות לפני מדידה.** ההפניה נשלחת מיד, ורישום הסריקה קורה אחריה. סורק שממתין
לכתיבה למסד נראה תקוע. אם רישום סריקה נכשל, מי שסרק לא מרגיש בכלל.

**בלי כתובות IP.** נשמרים רק מדינה, סוג מכשיר וחותמת זמן. די כדי להבין מאיפה
סורקים, ולא מספיק כדי לזהות אדם.

**זמן תגובה בסריקה הראשונה.** פונקציה שלא רצה זמן מה עולה מחדש, וזה מוסיף
כשנייה־שתיים. אם זה יפריע בשימוש אמיתי, אפשר להגדיר מופע מינימלי אחד —
זה מבטל את ההשהיה אבל עולה כדולר־שניים בחודש. משנים ב‑`functions/index.js`:

```js
{ region: 'us-central1', minInstances: 1 }
```

**המזהה קצר בכוונה.** חמישה תווים, בלי `0/O` ובלי `1/l/I`. אורך הכתובת משפיע
ישירות על צפיפות הקוד המודפס, והתווים המבלבלים הוסרו כי מישהו יקליד את הכתובת
ידנית כשהסריקה לא תעבוד.

**דומיין קצר משפר את הקוד.** `qr-generator-92987.web.app/r/ab3kf` הוא 38 תווים.
דומיין משלך כמו `qr.co.il/r/ab3kf` הוא 16 — מה שמייצר קוד דליל ופשוט יותר לסריקה.
זה שיפור טכני אמיתי, לא רק מיתוג. אם תרצה, מוסיפים דומיין כאן:

🔗 https://console.firebase.google.com/project/qr-generator-92987/hosting/main

---

## שני מצבים, בכוונה

האפליקציה מבטיחה שקוד רגיל נוצר במלואו במכשיר ואינו נשלח לשום מקום. קוד דינמי
שובר את ההבטחה הזאת מעצם טבעו — הוא **חייב** להישמר בשרת כדי שאפשר יהיה לשנות
אותו אחר כך.

לכן שני המצבים נפרדים בממשק, והרגיל הוא ברירת המחדל. משתמש שלא נוגע ב"קוד דינמי"
לא טוען בכלל את ספריות ההתחברות ומסד הנתונים, ולא שולח שום בקשת רשת.
